#include "license_pbs.h" /* See here for the software license */
#include <stdlib.h>
#include <stdio.h> /* fprintf */

#include "attribute.h" /* attropl */
#include "u_memmgr.h" /* memmgr */
#include "u_hash_map_structs.h" /* job_data */

int diswcs(int stream, const char *value, size_t nchars)
 {
 fprintf(stderr, "The call to diswcs needs to be mocked!!\n");
 exit(1);
 }

int encode_DIS_attropl_hash(int sock,memmgr **mm, job_data *job_attr, job_data *res_attr)
 {
 fprintf(stderr, "The call to encode_DIS_attropl needs to be mocked!!\n");
 exit(1);
 }


